# Work Detail Routes

- Public work detail pages live under `app/work/[id]`.
- Keep data reads server-side through `lib/db.ts`; do not add client fetching unless interaction requires it.
- Keep visual language aligned with the home portfolio page.
